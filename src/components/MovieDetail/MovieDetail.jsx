import { useSelector } from "react-redux";
import { useEffect, useState } from "react";
const MovieDetail = () => {
  

  return (
    <div>
     MovieDetail
    </div>
  );
};

export default MovieDetail;
